/**
 * Get the System's configuration information
 */
export declare const getSystemConfig: () => Promise<{
    [key: string]: unknown;
    logins: {
        [key: string]: unknown;
    };
    /**
     * List of available providers (logins) that are available
     */
    availableProviders: {
        provider: string;
        name: string;
        isOauth: boolean;
    }[];
}>;
/**
 * Run a pixel string
 *
 * @param pixel - pixel
 * @param insightId - id of the insight to run
 */
export declare const runPixel: <O extends [] | unknown[]>(pixel: string, insightId?: string) => Promise<{
    errors: string[];
    insightId: string;
    pixelReturn: {
        isMeta: boolean;
        operationType: string[];
        output: O[number];
        pixelExpression: string;
        pixelId: string;
        additionalOutput?: unknown;
        timeToRun: number;
    }[];
}>;
/**
 * Asyncronously run a pixel string
 *
 * @param pixel - pixel
 * @param insightId - id of the insight to run
 */
export declare const runPixelAsync: (pixel: string, insightId?: string) => Promise<{
    jobId: any;
}>;
/**
 * @name getPixelAsyncResult
 * @description Gets results for async pixel calls
 */
export declare const getPixelAsyncResult: <O extends [] | unknown[]>(jobId: string) => Promise<{
    errors: string[];
    insightId: string;
    results: {
        operationType: string[];
        output: unknown;
        pixelExpression: string;
    }[];
}>;
/**
 * Allow the user to login
 *
 * @param username - username to login with
 * @param password - password to login with
 * @returns true if successful
 */
export declare const login: (username: string, password: string) => Promise<boolean>;
/**
 * Allow the user to login with outh
 *
 * @param provider - provider to login with
 * @param isPopup - check if in popup
 * @returns true if successful
 */
export declare const oauth: (provider: string, isPopup?: boolean) => Promise<boolean>;
/**
 * Allow the user to logout
 *
 * @returns true if successful
 */
export declare const logout: () => Promise<boolean>;
/**
 * Upload file(s) to the backend
 * @param files
 * @param insightId
 * @param projectId
 * @param path
 * @returns
 */
export declare const upload: (files: File | File[], insightId: string | null, projectId: string | null, path: string) => Promise<{
    fileName: string;
    fileLocation: string;
}[]>;
/**
 * Download a file by using a unique key
 *
 * @param insightId - insightId to download the file
 * @param fileKey - id for the file to download
 */
export declare const download: (insightId: string, fileKey: string) => Promise<void>;
/**
 * Get a partial result from the insight
 * @param insightId - id of the insight to run
 */
export declare const partial: (insightId: string) => Promise<{
    message: {
        new: string;
        total: string;
    };
    status: string;
}>;
/**
 * Get the console message from an insight
 * @param insightId - id of the insight to run
 */
export declare const console: (insightId: string) => Promise<{
    message: string[];
    status: string;
}>;
/**
 * Get the console message from an insight
 * @param insightId - id of the insight to run
 */
export declare const getPixelConsole: (insightId: string) => Promise<{
    message: string[];
    status: string;
}>;
